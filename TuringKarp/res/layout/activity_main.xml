<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/alan_turing"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/turing_title"
        android:layout_centerHorizontal="true"
        android:textSize="36sp"
        android:layout_margin="8dp" />

    <ImageView
        android:id="@+id/alan_turing_image"
        android:contentDescription="@string/turing_contentDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/alan_turing"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        android:layout_margin="8dp"
        android:src="@drawable/alan_turing" />

    <ScrollView
        android:id="@+id/alan_turing_text_scroll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/alan_turing_image"
        android:layout_centerHorizontal="true"
        android:layout_margin="8dp" >

        <TextView
            android:id="@+id/alan_turing_text"
        	android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:text="@string/turing_text"
        	android:textSize="22sp" />
        
    </ScrollView>
    
</RelativeLayout>
